### Import

`import { Amend } from '../../../components';`

### Basic usage

This will display a bax to show to the user the diff about the current amend and the original text.

```tsx
import { amendMock, textMock } from '../../../../interfaces'
;<Amend amend={amendMock} text={textMock} />
```
